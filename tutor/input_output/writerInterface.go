package main

import "fmt"

type phoneWriter=struct{}

func (p phoneWriter) Write(bs []byte) P(int,error){
	if len(bs) == 0{
		return 0,nil
	}
	for i:=0;i<len(bs);++{
		if(bs[i]>='0' && bs[i]<='9'){
		}
	}

































































}
